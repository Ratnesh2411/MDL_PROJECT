steps:
  # Step 1: Install dependencies from 'requirements.txt' using pip
  - name: 'gcr.io/cloud-builders/pip'
    args: ['install', '-r', 'requirements.txt']

  # Step 2: Run the Python application
  - name: 'python'
    args: ['app.py']
